dbt_energy:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: "{{ env_var('GCP_PROJECT_ID') }}"
      dataset: dbt_marts 
      threads: 4
      keyfile: "{{ env_var('GCP_CREDENTIALS') }}"