_weather__sourceversion: 2

sources:
  - name: weather_data
    schema: weather_data
    tables:
      # 1. The clean name you want to use in your dbt SQL
      - name: forecasted_weather
        # the actual name of the table in BigQuery
        identifier: forecast
      - name: historical_weather
        identifier: historical
